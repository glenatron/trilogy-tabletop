import { FocusListDirective } from './focus-list.directive';

describe('FocusListDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusListDirective();
    expect(directive).toBeTruthy();
  });
});
